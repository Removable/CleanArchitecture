namespace CleanArchitecture.Domain.Common.Interfaces;

public interface IAggregateRoot
{
    // This interface is intentionally left empty.
    // It serves as a marker interface to indicate that an entity is an aggregate root.
    // Aggregate roots are the main entry point for accessing and modifying the aggregate's state.
}
